import { SlideColumnDirective } from './slide-column.directive';

describe('SlideColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideColumnDirective();
    expect(directive).toBeTruthy();
  });
});
